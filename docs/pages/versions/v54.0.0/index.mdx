

import React from "react";
import { NavigationContainer } from "@react-navigation/native";
import { createBottomTabNavigator } from "@react-navigation/bottom-tabs";
import { View, Text, TouchableOpacity } from "react-native";
import { Ionicons } from "@expo/vector-icons";

const Tab = createBottomTabNavigator();

function HomeScreen() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center", backgroundColor: "#fff" }}>
      <Text style={{ fontSize: 24, fontWeight: "bold", marginBottom: 20 }}>🚀 Curiuar</Text>
      <TouchableOpacity style={{ backgroundColor: "#2563eb", padding: 15, borderRadius: 20 }}>
        <Text style={{ color: "#fff", fontSize: 16, fontWeight: "600" }}>📦 बुक डिलीवरी</Text>
      </TouchableOpacity>
    </View>
  );
}

function TrackScreen() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center", backgroundColor: "#fff" }}>
      <Text style={{ fontSize: 18, fontWeight: "bold" }}>📍 लाइव ट्रैकिंग</Text>
      <Text style={{ marginTop: 10, color: "gray" }}>(यहाँ मैप दिखेगा)</Text>
    </View>
  );
}

function ProfileScreen() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center", backgroundColor: "#fff" }}>
      <Text style={{ fontSize: 18, fontWeight: "bold" }}>👤 मेरी प्रोफ़ाइल</Text>
      <Text style={{ marginTop: 10, color: "gray" }}>नाम, मोबाइल, पेमेंट सेटिंग्स</Text>
    </View>
  );
}

export default function App() {
  return (
    <NavigationContainer>
      <Tab.Navigator
        screenOptions={({ route }) => ({
          tabBarIcon: ({ color, size }) => {
            let iconName;
            if (route.name === "होम") {
              iconName = "home";
            } else if (route.name === "ट्रैकिंग") {
              iconName = "location";
            } else if (route.name === "प्रोफ़ाइल") {
              iconName = "person";
            }
            return <Ionicons name={iconName} size={size} color={color} />;
          },
          tabBarActiveTintColor: "#2563eb",
          tabBarInactiveTintColor: "gray",
          headerShown: false,
        })}
      >
        <Tab.Screen name="होम" component={HomeScreen} />
        <Tab.Screen name="ट्रैकिंग" component={TrackScreen} />
        <Tab.Screen name="प्रोफ़ाइल" component={ProfileScreen} />
      </Tab.Navigator>
    </NavigationContainer>
 
);
}
